#!/bin/sh

rm -f config.make

echo "" >> config.make
echo "PREFIX   = ${PREFIX:-/usr/local}" >> config.make
echo "LIBDIR   = ${LIBDIR:-lib}" >> config.make
echo "" >> config.make
echo "ARCH     = ${ARCH:-C}" >> config.make 
echo "" >> config.make
